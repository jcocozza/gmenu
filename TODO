- implement a fuzzy search
- exec flag?
    - run a the "exec" command with the selected value passed in
- support inputting file directly
    - read the file line by line and make those the choices
- an "alias" flag that supports the "alias" format
when reading, gmenu will parse each line as <item name> <item value>.
The user then selects from the item names and the item value is returned

e.g.:

google www.google.com
github www.github.com

selecting google will return www.google.com
